# Semantic Extraction Modules — Dependencies
# Install: pip install -r requirements.txt

# LookML parser (official Looker package)
lkml>=1.3.0

# HTTP client for REST APIs (Tableau, Looker, Power BI, BO, Denodo Catalog)
requests>=2.28.0

# YAML output
pyyaml>=6.0

# XML bomb protection for Tableau TWB/TDS parsing
defusedxml>=0.7.1

# Power BI .pbix parsing (optional — used when .pbit/.pbip/.bim not available)
# Uncomment if needed:
# pbixray>=0.1.0

# JDBC bridge for Denodo VDP (optional — only for live VDP connections)
# Requires Java runtime
# Uncomment if needed:
# jaydebeapi>=1.2.3
